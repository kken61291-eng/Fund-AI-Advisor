name: Data Crawler (Batch Updater)

on:
  schedule:
    # 北京时间 08:30 - 15:30 每小时爬取一次
    - cron: '30 0-7 * * 1-5'
  workflow_dispatch:

concurrency: 
  group: data-crawler
  cancel-in-progress: false

jobs:
  update_data:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🕷️ Run Market Data Crawler
      env:
        # 如果爬虫需要 Tushare，请保留这个，不需要邮件变量
        TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
      run: |
        python batch_updater.py

    - name: 📡 Run News Crawler
      run: |
        python news_loader.py

    - name: Commit and Push Data
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        
        if [[ -n $(git status --porcelain data_cache/ data_news/) ]]; then
          git add data_cache/*.csv
          git add data_news/*.jsonl
          git commit -m "💾 Auto-save market & news data [skip ci]"
          git pull --rebase origin main
          git push
        else
          echo "No new data fetched."
        fi
