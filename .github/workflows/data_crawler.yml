name: Daily Data Crawler

on:
  schedule:
    # 每小时第 0 分钟运行 (抓取数据)
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  fetch_data:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
        # [关键] 强制升级 akshare 到最新版，防止 7x24 接口失效
        pip install --upgrade akshare

    - name: 🕷️ Run News Crawler
      run: |
        python news_loader.py

    - name: 💾 Commit and Push Data
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        
        # 添加 data_news 目录下的所有变动
        git add data_news/
        
        # 检查是否有文件变化，有则提交，无则跳过
        if [[ -n $(git status --porcelain data_news/) ]]; then
          git commit -m "📡 Auto-update news data [skip ci]"
          git push
        else
          echo "No new data found, skipping commit."
        fi
